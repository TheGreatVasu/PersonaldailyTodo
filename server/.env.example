PORT=3001
# Comma-separated origins allowed to call the API (your Vercel site)
# Example: https://your-app.vercel.app,https://www.yourdomain.com
CORS_ORIGINS=
# Optional: persistent disk path on Render (mount disk then set e.g. /var/data)
# DATA_DIR=
